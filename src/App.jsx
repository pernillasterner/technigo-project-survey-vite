import { FormBody } from "./components/Form/Form";

/**
 * Need to send this data to form
 * - id
 * - question info
 * - question body
 *
 */

export const App = () => {
  return (
    <div className="form">
      <FormBody />
    </div>
  );
};
